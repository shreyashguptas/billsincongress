# Dependencies
node_modules

# Next.js
.next
out
# Auto-generated by next dev/build/typegen; path flips between dev/prod builds
next-env.d.ts

# Environment
.env
.env*
!.env.example
.env.local
.env*.local
.env*.bak

# Local agent / coding-assistant scratch. These are per-machine tool state,
# not project source; they kept getting committed by accident.
.agents/
.claude/
.codex/
.deepsec/
skills-lock.json
# Per-tool copy of the house rules. The committed, tool-neutral copy is AGENTS.md.
CLAUDE.md

# Plans and specs are never committed — write them to disk untracked.
/docs/
/*-plan.md

# Credentials, in whatever shape a tool decides to write them.
credentials*.json
service-account*.json
*.pem
*.key
id_rsa*

# IDE
.cursorignore
.cursor/

# OS
*.DS_Store

# Old data files
api-testing/
failed_bills_*.json
images-of-representatives.json

# Build
tsconfig.tsbuildinfo

# Cloudflare / OpenNext
.open-next/
.wrangler/
cloudflare-env.d.ts

# Misc
/extra-files-for-future
